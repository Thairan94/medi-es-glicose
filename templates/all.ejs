<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Links</title>
    <link rel="stylesheet" 
    href="https://stackpath.bootstrapcdn.com/bootstrap/4.1.3/css/bootstrap.min.css" 
    integrity="sha384-MCw98/SFnGE8fJT3GXwEOngsV7Zt27NXFoaoApmYm81iuXoPkFOJwJ8ERdknLPMO" 
    crossorigin="anonymous">
    <link href="https://fonts.googleapis.com/icon?family=Material+Icons"
      rel="stylesheet">
</head>
<body>
    <div class="container">
        <h1>Medições</h1>
        <a href="/add">Adicionar</a>
        <% for(let i=0; i < links.length; i++) { %>
            <div class="card p-4" id='<%=links[i]._id%>'>
                <form class="d-flex align-items-center justify-content-between" method="POST" action="/?_method=DELETE">
                    <div class="d-flex flex-column">
                        <h2><%=links[i].title%></h2>
                        <div><%=links[i].description%></div>
                        <div class="text-secondary"><%=links[i].url%></div>
                        <div class="text-muted"><i class="material-icons">bar_chart</i>Clicks: <%=links[i].click%></div>
                    </div>
                    <div class="d-flex flex-column">
                        <input name="id" value="<%=links[i]._id%>" hidden>
                        <a class="btn btn-info mb-2" href="/edit/<%=links[i]._id%>">Edit</a>
                        <button class="btn btn-danger" type="submit">Delete</button>
                    </div>
                </form>
            </div>
        <%} %>
    </div>

   
</body>
</html>